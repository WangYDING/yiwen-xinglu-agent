# R6 真实导师 Pilot 离线方案

状态：已准备，未运行。模型冻结为 `deepseek-chat`，四场景为初次课程与提示、错误诊断后补课、考试失败解释、传承拒绝与授予。建议总预算上限为 **0.05 CNY**。

未来运行必须显式提供 `--confirm-paid-run`，`/models` 最多一次，每场景 Chat 最多一次，无整轮自动重试，单次格式/契约修复最多一次，30 秒超时；用量无法核对即冻结。结果脱敏，原始结果进入 Git 忽略目录，不保存 Prompt，不输出 API Key。

准入线为四场景完整响应、Schema 合法、零病例工具、零非法写入、零隐藏/答案泄漏、零权限绕过、规则决定一致且历史引用可追溯。失败必须保留。单次结果只能称“工程案例验证”。

未来准确授权文本：

> 授权执行R6 DeepSeek真实导师四场景Pilot，允许DeepSeek `/models`至多1次、每场景Chat至多1次，总预算上限0.05 CNY，并要求使用已冻结 `real_mentor_pilot_v1`；除此之外不得访问外部服务。
