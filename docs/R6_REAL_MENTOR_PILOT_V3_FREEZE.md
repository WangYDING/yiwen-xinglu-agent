# R6 真实导师 Pilot v3 预冻结

状态：**已离线预冻结，未授权、未运行真实模型**。

v3 保持 v2 的 `deepseek-v4-flash`、thinking disabled、JSON、512 输出 Token、`0.05 CNY` 建议上限、价格快照、五个基础场景、权威状态、规则决定和安全门槛。允许变化仅为确定性 CommunicationPlan、`MentorActionV2.covered_point_ids`、逐点正文评测、完整 fallback 和正确停止分类。

质量失败记录模型失败、交付确定性 fallback，并继续后续独立请求；fallback 不计作模型通过。协议、契约两次失败、安全、预算、超时和供应商身份失败立即停止。真实运行仍需单独付费授权。
