# R6 普通玩家公开展示边界

状态：离线修复已完成；未重新调用真实模型，尚未经过真人试玩。

## 边界与设计

权威存储、领域事件、幂等键、规则关联和隐藏表单继续使用稳定内部 ID。普通玩家可见文字与 Mentor 自然语言事实统一经过版本化 `public_terminology_v1`：每项保存实体类型、内部 ID、公开名称和公开短说明；重复映射拒绝加载，未知值只返回通用公开说明，不回显输入。

当前覆盖六项能力、六门课程、三个补课、四个教学阶段、六级权限、第一条传承、正式考试、推荐终态和原因码、教学阶段、病例结局及三维关系。展示配置只投影文字，不参与规则决定。

## Mentor 输入与输出

医馆正式支持的五类表达保持冻结的五个 CommunicationPlan point 及教学语义。补课、考试与传承授予场景在进入计划前改为公开对象，例如“辨证”和“辨证与诱饵排除补课”，不再把能力、补课或权限内部 ID 作为自然语言事实。

模型正文显示前按可信目录的完整内部 ID 集扫描。普通 ID 命中记为 `presentation_quality_failure`，该次模型表达不算通过，页面使用确定性公开 fallback，且不写产品状态；隐藏权限、答案或秘密门槛仍升级为安全停止。合法隐藏表单值与内部存档不属于玩家可见正文。

## 页面、CLI 与记忆

医馆首页、课程、补课、考试、权限、传承和师评改用公开名称。CLI 的推荐、补课、阶段、权限、能力和关系变化、考试与传承结果也使用公开文字，并隐藏玩家无需理解的稳定 ID。新结构化记忆摘要直接写公开名称；历史记录不改写，在读取投影时安全转换，权威来源 ID、幂等和哈希语义保持不变。

## 历史烟雾离线重评

历史原始证据及 SHA-256 `54e091489d092ea32b9806b56d07de41e2eb201cd380866d0b77bef64c36fdcb` 未修改。离线重评确认历史文本中的 `reason_diagnosis`、两个补课 ID 和英文权限值会被新防线识别；当前投影改为公开名称或 fallback。

结论：真实烟雾的模型、安全和状态结果保持不变；展示缺陷已在后续离线版本修复，修复尚未经过新的真人试玩。这不是一次新的真实 API 验证。

## 验证边界

专项测试覆盖目录完整性、未知 ID、安全降级、五类 Mentor 公开事实、模型泄漏、可见 HTML 与隐藏字段区分、历史证据重评、结构化记忆及既有医馆 Mock 回归。本轮 `/models`、Chat、BGE、Embedding、真人联系和远程操作均为 0，费用为 0 CNY。
