# R6 离线产品验收

状态分层：工程完整性通过；确定性产品闭环通过；Fake Mentor 离线规则路径通过；真实模型未运行；真人试玩未执行；远程发布未执行；R6 整体进行中。

冻结来自 `9b0a96c` 的 `product_acceptance_v1`，冻结内容未修改。执行前以 `5bb264b` 的通用 Schema 修复关闭 R5 等价调查缺口，该修复只让产品满足既有门槛，不改变 Gold、真相、评分、成长、考试或传承门槛。

八条路线覆盖优秀六案主线、错误诊断补课、危险处置守则、考试失败重考、传承拒绝授予、无传承古祠、有传承替代路径及双玩家隔离。普通与传承古祠均为 100 分；传承路径实际少一次普通调查。

两次相同优秀主线在独立临时目录运行，规范化比较病例、成长、教学、结构化记忆、考试、权限与公开终态；排除目录、时间、PID及随机教学/记忆标识。机器结果位于 Git 忽略的 `results/r6_offline/`，运行结束临时状态已清理。

性能仅为本机 Windows Python 3.12 单次观察，不是并发、云端或生产 SLA；不用于反向修改 Gold。准确数值以机器结果为准。
