# R6 真实导师 Pilot v1 停止记录

v1 固定为 `deepseek-chat`，但仓库当时只有 `deepseek-v4-flash` 的价格、预算与 DoctorAgent 传输边界。它因此在任何 `/models` 或 Chat 请求前停止，四场景均为 `not_observed`，费用为 `0 CNY`。

该记录保留，不通过修改 v1 抹除历史。后续可执行方案必须以新版本冻结；`product_acceptance_v1`、v1 场景主题和安全门槛不因此改变。
