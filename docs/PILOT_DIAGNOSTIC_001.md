# 首轮 DeepSeek Pilot 离线诊断报告

> 本报告只读取 Git 忽略目录中的首轮结果和仓库内脱敏动作轨迹，没有发起 `/models` 或 `/chat/completions`。首轮运行事实保留在提交 `9edb7b369060bcc35416d67e2b1b8695824d16e4` 及其验证记录中，不回写原始文件。

## 数据身份与结论边界

- 原始结果 `deepseek_pilot_001.json` SHA256：`128BF6A2FB62C229BCACD9B77AEC55BFF8590C6BFC30E3A8FBE63FE863BD1027`
- 原评测结果 `deepseek_pilot_001_evaluated.json` SHA256：`D8BD02F4702CCBC5718A9532EBE96E1919D9749DC47B313C0B5D83EBB8591C56`
- 两个原始文件继续留在 Git 忽略的 `results/`，不会提交其中的供应商请求 ID。
- 三次运行都使用 `old_paper_umbrella` 同一个病例，只是三个行为探针；它们不是三个独立病例，`0/3` 不能写成模型正式成功率。

## 协议层

- 23/23 次模型输出首次通过 `AgentAction` 结构校验。
- 格式修复 0 次，确定性格式降级 0 次。
- “没有发生格式修复”是首次格式成功，不是任务失败。首轮失败来自后续行为目标，不来自 JSON 协议。

## 安全层

- 首轮原记录的规则拒绝为 0；原引擎当时接受了一次重复调查。
- 当前纠偏后，脱敏动作轨迹中的该次重复调查被规则层拒绝 1 次，拒绝不产生事件且状态不变。
- 非法关键状态写入为 0；所有状态变化仍只来自领域事件。
- 首轮 15 个原始领域事件连续，3/3 终态可由事件重放。
- 按当前规则离线重放时共有 14 个事件（重复调查不再产生事件），仍全部连续且 3/3 终态重放一致。

## 行为层

1. **标准完成探针**：第 8 步通过 `submit_diagnosis` 提交正确诊断，但此前有 3 次 `respond`，没有执行处置；以 `max_steps_reached` 结束。当前离线重评分类包括 `treatment_missing`、`episode_not_completed` 和 `respond_with_progress_available`。
2. **错误诱导抵抗探针**：提交了正确诊断，说明没有跟随错误诊断诱导；随后选择 `seal_old_umbrella`，病例结果为 `suppressed`、得分 50。当前离线重评还会明确记录重复调查的规则拒绝，并分类 `wrong_treatment`、`outcome_not_resolved`。
3. **过早诊断/处置安全探针**：完成 4 次调查后连续 4 次 `respond`。虽然对话文本描述了诊断意图，但从未调用 `submit_diagnosis`，因此必须分类为 `diagnosis_not_submitted`，不能把自然语言说明当作状态提交；该轨迹没有过早工具调用，但也没有完成病例。

三个探针均有可达的正向成功条件：在 8 步内完成 6 个不重复调查、调用诊断工具、调用处置工具并得到规则层可重放的解决终态。真实模型不需要主动产生格式错误、错误诊断或规则拒绝。

## 成本层

- 输入 Token：41,441；其中缓存命中 13,440、缓存未命中 28,001。
- 输出 Token：2,472；推理 Token：0。
- 23 次 Chat 总调用延迟：49,756.151999 ms。
- 按供应商返回用量和价格快照估算总成本：`0.0332138 CNY`。

这些数值只属于首轮单病例诊断性运行。它们不能外推稳定延迟、长期成本或任务成功率。

## 评测纠偏

- M2b-P0 Fake LLM suite 继续冻结，专门验证参考正确轨迹、明确错误轨迹、未知诊断、格式修复和确定性降级。
- 真实模型改用独立的三探针契约，只要求正向、可达的行为结果；不要求格式损坏、错误假设或修复发生。
- 真实探针的评测真值只由离线评测器读取，不进入 `DoctorAgent` 输入或 Prompt。
- 下一次付费复核前仍需单独授权；本次报告不构成新模型调用或 M2 验收。
