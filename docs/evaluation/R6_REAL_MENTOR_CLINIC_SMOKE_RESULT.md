# R6 单人真实导师医馆受控烟雾结果

执行状态：已完成；这是开发者控制的单人产品链路烟雾，不是稳定成功率或真人体验证明。

## 身份与范围

- 执行 HEAD：`fefb7c8b99686bf5ca8f7d358c1aaf90b206ad51`
- 模型：`deepseek-v4-flash`；thinking：`disabled`
- 脱敏命令：`xuanyi-clinic --mentor-mode deepseek --confirm-paid-run --budget-cny 0.05 --state-dir <ignored-smoke-state> --port 0`
- `/models`：1 次；基础 Chat：5 次；修复 Chat：0 次；没有自动重跑或超时重试。
- 两个独立服务仅监听 `127.0.0.1`，实际动态端口为 `49913` 和 `49929`。进程1完成前四项后退出，进程2恢复相同存档及预算账本并完成第五项；没有孤儿医馆进程。Windows 控制台中使用 CTRL_BREAK 正常中止服务循环，进程返回码 `3221225786`，两个 stderr 均为空。

## 五类产品链路结果

五次表达均由 loopback HTTP 页面/表单进入 `ClinicService`，从公开权威状态生成 CommunicationPlan，经 MentorActionV2、契约和安全检查后显示在页面；没有调用 Pilot runner。

| 交互 | HTTP/页面 | 契约点覆盖 | 模型结果 | fallback | 内容观察 |
|---|---|---:|---|---|---|
| 初课与提示 | 200 / 正常显示 | 5/5 | 首次通过 | 否 | 说明弟子亲自行事、导师不接管，并给出有限公开提示；无答案 |
| 错误诊断补课 | 200 / 正常显示 | 5/5 | 首次通过 | 否 | 准确说明诊断错误、不增加能力、补课及后续表现；但页面正文暴露 `remediate_diagnostic_reasoning_v1` 和 `reason_diagnosis` 内部标识 |
| 考试失败 | 200 / 正常显示 | 5/5 | 首次通过 | 否 | 准确说明未通过、补课后才能重考、导师不能改分；但页面正文暴露 `reason_diagnosis` 和 `remediate_treatment_alignment_v1` 内部标识 |
| 传承拒绝 | 200 / 正常显示 | 5/5 | 首次通过 | 否 | 只说明考试、认可等公开类别，规则不可绕过，可补足后再申请；无精确门槛 |
| 传承授予 | 200 / 正常显示 | 5/5 | 首次通过 | 否 | 明确“溯契还因”由规则授予，语言不写权限，仍须玩家判断；正文使用英文公开权限词 `inheritance` |

统计：首次通过 5，修复后通过 0，fallback 0，模型失败 0，未观察 0。契约评测虽全部通过，但普通玩家页面的内部标识暴露是独立的产品文案缺陷，因此本烟雾不建议直接进入真人试玩。

## 页面、安全与状态

- 医馆主页显示真实 DeepSeek、可用状态、已用费用、剩余预算和 fallback 状态；交互子页显示导师正文，但不重复显示模式和预算卡。
- 刷新首次表达结果页未增加请求计数，证明 GET 刷新不重复计费。
- 页面未出现完整 Prompt、point ID、Schema、内部哈希、供应商请求 ID、API Key、异常栈、隐藏线索、考试答案或精确传承门槛。
- 隐藏信息泄漏、答案泄漏、越权、替玩家行动、AgentAction/病例/MCP 工具和非法状态写入计数均为 0。
- 模型表达前后未产生领域事件。初课和补课场景最初的文件级哈希因只读 `home()` 协调了既有权限状态而变化；独立 Fake 基准重放相同正式操作后，病例、成长、关系、课程、补课、结构化记忆、考试、权限、传承和 Campaign 摘要对三个玩家全部相等。

## 预算、Token 与延迟

- 授权及生效预算：`0.05 CNY`
- 已确认实际费用：`0.00294896 CNY`
- 未核对预留：`0 CNY`
- 运行结束最大承诺费用：`0.00294896 CNY`
- 剩余预算：`0.04705104 CNY`
- 输入 Token：3,734，其中缓存命中 2,048、缓存未命中 1,686；输出 Token：611。
- 五项延迟依次约为 2147.22、1973.35、1662.95、2166.88、2044.49 ms；总计约 9994.90 ms。
- 第二进程恢复相同本地 run ID、已确认费用、请求数和未冻结状态；最终基础请求数 5、修复数 0、frozen=false。

## 证据与结论

原始页面、脱敏运行指标、预算账本和本地状态保存在 Git 忽略目录 `results/r6_real_mentor_clinic_smoke/`。原始证据包 SHA-256：`54e091489d092ea32b9806b56d07de41e2eb201cd380866d0b77bef64c36fdcb`。不提交完整响应、Prompt、API Key、供应商请求 ID、烟雾玩家存档或自由文本。

结论：正式医馆到真实 MentorActionV2 的五类链路、预算跨进程恢复、fallback 边界和权威状态隔离已得到一次工程烟雾证据；内部标识仍会出现在两类玩家页面，需监督窗口另行授权离线产品修复与 Mock 回归后再决定是否进入 3～5 人试玩。真人试玩、BGE/Embedding 和远程发布均未执行。

## 后续离线修复说明（不改写历史观察）

上述内部 ID 暴露是本次烟雾当时真实存在的缺陷，原始证据、SHA、五请求结果和观察表均保持不变。后续版本新增统一公开术语映射与显示前扫描防线，并以 Mock 和保存文本离线重评确认：已知内部 ID 会触发 `presentation_quality_failure` 与确定性公开 fallback，页面、CLI 和结构化记忆投影改用公开名称。该结论不表示烟雾当时通过了展示门槛，也不构成新的真实 API 或真人试玩验证。详见 [`R6_PUBLIC_PRESENTATION_BOUNDARY.md`](R6_PUBLIC_PRESENTATION_BOUNDARY.md)。
