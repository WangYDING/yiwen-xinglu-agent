# 架构决策记录

本文件记录会影响实现边界或后续返工成本的决策。新决策追加记录，不静默改写历史结论。

## ADR-001：确定性领域层拥有关键状态

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：病例真相、能力值、关系值、权限、动作合法性、技能解锁和最终状态修改只能由确定性领域层负责。Agent 只能输出经过校验的语言、工具请求或状态变化建议。
- **原因**：避免模型幻觉直接污染游戏状态，使行为可测试、可审计和可回放。

## ADR-002：M0 使用 Pydantic v2 和 `src` 布局

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：领域对象使用 Pydantic v2；项目采用 `src/xuanyi_npc` 包布局，测试使用 pytest。
- **原因**：Pydantic 同时提供运行时校验、JSON 序列化和后续结构化输出 Schema，`src` 布局可避免测试误用仓库根目录中的未安装模块。

## ADR-003：M0 使用原子 JSON 文件验证持久化接口

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：M0 仅为玩家状态和病例会话提供原子 JSON 保存/读取接口，运行数据写入被 Git 忽略的 `runtime_data`；SQLite 延后到需要事件查询和记忆检索时引入。
- **原因**：先验证模型边界和存储接口，避免在领域模型尚未稳定时绑定数据库表结构。

## ADR-004：首个病例是可替换的技术夹具

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：“旧纸伞与失约书生”仅用于验证线索、调查、误导、诊断、处置和评分结构，不作为最终世界观承诺。患者和灵体在垂直切片阶段不是独立 Agent。
- **原因**：优先完成一个可测试闭环，减少世界观未决事项对工程进度的阻塞。

## ADR-005：MCP 不包含业务规则

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：后续 MCP 工具只调用应用服务；动作校验和状态更新继续由领域层实现，不在 MCP Server 中复制规则。
- **原因**：保证命令行、测试、未来界面和 MCP 共用同一套确定性行为。

## ADR-006：M1 使用命令—事件—新状态执行模型

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：`CaseEngine.execute()` 接收病例、玩家、当前会话和一个带时区时间戳的结构化命令，返回新会话、领域事件及用户可见结果。引擎不原地修改输入；失败通过具体 `RuleViolation` 子类型返回，且不产生部分状态。
- **原因**：保证同样输入得到相同结果，并为后续日志、MCP 工具、回放和故障分析提供稳定边界。

## ADR-007：M1 的病例评分保持确定且可解释

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：当前总分由已发现关键线索占比、诊断正确分、根因处置分和危险处置惩罚构成，并限制在病例配置的最高分内。提示扣分、心性评价、技能成长和关系变化推迟到教学阶段。
- **原因**：先验证病例过程与结果的评分闭环，避免在教学和关系规则确定前混合多个尚未实现的维度。

## ADR-008：Agent 上下文采用检索前最小权限视图

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：模型只能接收 `PlayerView` 与 `CaseObservation`。视图构建时只加入当前已经解锁或发现的内容，并移除病例根因、因果链、未发现线索、隐藏信息、合法诊断集合、处置结果、评分规则、技能前置条件及关系原始值。
- **原因**：秘密不能在进入 Prompt 后再依赖提示词保护；未授权内容必须在模型调用前从数据层移除。

## ADR-009：V0、V1、V2 共用 EpisodeResult 并采用递增能力边界

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：V0 使用短期上下文、固定课程、结构化行动和基础工具；V1 只在 V0 上增加经过验证的跨回合记忆；V2 再增加自适应教学和 Reflection。三个版本共用 `EpisodeResult`，确保后续对比实验的输出结构一致。
- **原因**：避免版本间同时改变多个变量，使记忆和教学能力的增益能够通过真实消融实验复核。

## ADR-010：模型不能直接提交永久记忆或关键状态

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：从 Agent 工具集合移除永久 `record_memory`，并从 `AgentAction` 移除关系、技能状态变化提议。未来永久记忆必须由已验证领域事件经过记忆投影策略生成；模型文本最多作为待验证候选内容。
- **原因**：防止提示注入、幻觉或工具误用直接污染长期状态，并保留记忆来源以供审计。

## ADR-011：事件重放是状态一致性的发布门槛

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：病例引擎产生的事件必须能够从同一初始 `CaseSessionState` 重建完全相同的最终状态；事件序号断裂、会话不匹配、结束后事件和无证据诊断事件必须拒绝重放。
- **原因**：保证日志不仅可查看，还能用于故障恢复、评测复核和状态一致性验证。

## ADR-012：公开历史不保留本地占位邮箱

- **状态**：已接受，推送前待执行
- **日期**：2026-08-03
- **决策**：当前本地提交使用 `wyd@localhost`，但首次公开推送前不保留该地址。取得用户确认的 Git 邮箱或 GitHub noreply 地址后，统一改写本地提交作者；提交哈希变化后同步更新所有记录和交付材料。未取得确认前禁止公开推送，不猜测用户邮箱。
- **原因**：避免作品集公开历史包含不可关联的占位身份，同时避免助手擅自写入错误个人信息。

## ADR-013：安全过滤不是产品消融变量

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：V0、V1、V2 全部强制启用 `AgentContextFilter`。“提示词保密 vs. 程序化过滤”仅作为离线 A4 安全消融，使用合成数据、禁用持久化和外部动作，并明确标记不可部署。
- **原因**：消融实验不能制造容易误部署或污染真实状态的不安全产品版本。

## ADR-014：V1 与 V2 使用不同复杂度的记忆检索

- **状态**：已接受
- **日期**：2026-08-03
- **决策**：V1 仅增加基础向量相似度 Top-K 长期记忆检索并保持固定课程；V2 才使用相关性、重要性、时间新近度等多因素记忆排序，同时加入自适应教学和 Reflection。
- **原因**：确保 V0→V1 主要测量基础长期记忆价值，V1→V2 再测量排序、教学适配和 Reflection 的联合增益。

## ADR-015：M2-V0 使用只读 Agent 提议与有界失败策略

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：V0 的 `DoctorAgent` 通过可替换 `LLMAdapter` 接收权限过滤后的 `PlayerView`、`CaseObservation`、最近有限条消息和只按步骤编号推进的固定课程。模型输出必须校验为 `AgentAction`，并使用应用层指定的本步 `action_id`；首次格式失败只允许一次修复请求，第二次失败或供应商调用失败时返回不含工具调用的确定性 `respond` 降级动作。模型不接收 `CaseDefinition`、可写会话或应用层时间戳。
- **原因**：建立可复现实验基线，同时限制格式错误、供应商异常和模型越权对状态机的影响。

## ADR-016：V0 工具层只翻译命令，不复制病例规则

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：V0 工具执行器使用严格参数模型，将 Agent 工具建议翻译成既有领域命令。调查目标和命令时间由应用层补齐；技能、线索、证据、处置前置条件及会话状态继续由 `CaseEngine` 校验。成功状态变化只来自引擎事件；拒绝只记录错误码，并向下一次模型调用提供不包含隐藏门槛细节的统一反馈。M2 不实现 MCP、长期记忆、自适应课程或 Reflection。
- **原因**：保证测试、未来界面和 MCP 共用同一规则来源，并防止模型通过伪造参数或错误消息侧信道改变、探测关键状态。

## ADR-017：M2 拆分为 M2a Harness 与 M2b 真实模型验证

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：当前 Fake LLM、结构化行动、只读上下文、规则工具和 Episode 闭环统一记为“M2a：Fake LLM Agent Harness 与安全闭环完成”。“M2b：真实 LLM 适配、3 条 dev 场景、小型 Pilot、真实超时/Token/延迟验证”尚未完成。在 M2b 验收前，不得把整个 M2 标记为完成，也不得开始 M3。
- **原因**：Fake 脚本能证明编排和安全边界可运行，但不能证明真实模型能理解公开语义、稳定选择工具或满足供应商运行约束。

## ADR-018：Agent 使用公开诊断词表，正确性仍由领域层持有

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：每个病例必须提供至少两个公开诊断候选，包含候选 ID 和中立公开说明，并同时包含至少一个有效候选和一个合理错误候选。内部 `root_cause` 与公开候选 ID 分离；`valid_diagnosis_ids`、候选正确性、评分和隐藏门槛不进入 Agent 视图。规则层接受词表内的错误候选并按既有规则计分，使用 `unknown_diagnosis` 拒绝词表外 ID，失败不产生状态变化。调查和处置也提供独立公开说明，但不复用会泄露结果或门槛的内部说明。
- **原因**：真实模型必须从安全上下文获得可提交标识，同时不能因接口可用性而泄露 Gold 答案或确定性世界真相。

## ADR-019：ROADMAP 与实验方案 v0.2 成为当前规划来源

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：仓库内 `docs/ROADMAP.md` 是唯一有效的里程碑状态来源；README、最新 ADR 和验证记录与其同步。Word 设计总结、`xuanyi-npc-handoff` 和实验方案 v0.1 保留为历史基线，不再表示当前执行状态。实验方案 v0.2 规定所有产品版本强制使用 `AgentContextFilter`，仅提示词保密只允许作为不可部署的隔离 A4 消融。
- **原因**：消除历史规划、实现状态与安全实验定义之间的冲突，避免把研究目标误写成工程完成条件。

## ADR-020：M2b 拆分为供应商无关 P0 与真实模型 P1

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：M2b-P0 先以严格数据 Schema 定义 3 条 dev 场景，为每条场景保存一条参考轨迹和至少一条明确错误轨迹，并由只接收统一 `EpisodeResult` 的确定性评测器分类。场景真值、成功条件和失败条件只存在于评测数据，不进入 Agent Prompt；Fake LLM 运行不填写延迟、Token、成本或真实成功率。M2b-P1 才实现用户选定供应商的真实 `LLMAdapter`，使用 P0 场景运行小型 Pilot 并采集真实指标。P1 完成前整个 M2 保持进行中，M3 禁止开始。
- **原因**：dev 数据与评测逻辑不依赖供应商选择，可以先冻结并测试；把真实适配和指标留到 P1，能够避免 Fake 结果冒充模型能力，也使后续不同供应商共用同一确定性门槛。

## ADR-021：真实数据产生前将 ModelUsage 改为供应商无关契约

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：删除只适用于美元的 `cost_usd`，统一记录输入、输出、缓存命中输入、缓存未命中输入、推理 Token、真实延迟、估算成本、ISO 币种、供应商返回模型、可选请求 ID 与系统指纹。估算成本和币种必须同时存在或同时缺省；Episode 只聚合同币种的完整成本。发生已收费但用量不完整的失败时保留已知用量并标记 `measurement_complete = false`，后续预算门禁停止新 Episode。Fake LLM 继续不创建 `ModelUsage`。
- **原因**：DeepSeek 按人民币及缓存分项计费，强制换算美元会引入不可复现的临时汇率；在首个真实 Pilot 前调整 Schema，不会迁移或污染任何真实实验记录。

## ADR-022：M2b-P1a 使用 DeepSeek 直接 HTTP Adapter 并隔离付费执行

- **状态**：已接受
- **日期**：2026-08-04
- **决策**：首轮供应商固定为 DeepSeek 官方 API，模型只允许 `deepseek-v4-flash`。Adapter 继续实现既有 `LLMAdapter`，通过 `httpx` 调用标准 `/chat/completions`，不使用供应商原生工具调用、Beta strict tools 或隐式重试；请求固定使用 JSON Output、非流式、温度 0 并关闭思考。P1a 只完成 MockTransport 离线验证、只读模型发现和预算门禁。真实 `/models` 与付费 Chat 请求属于 P1b，必须由用户再次授权，且 Pilot 命令还要求显式 `--confirm-paid-pilot`。
- **原因**：保持模型只提出 `AgentAction`、应用层执行工具的安全架构，同时把协议实现与会产生费用的实验执行拆开，避免预检阶段误用 Key 或产生费用。

## ADR-023：允许从项目根目录加载本地 `.env`

- **状态**：已接受
- **日期**：2026-08-06
- **决策**：DeepSeek 配置在未传入显式配置映射时，读取当前项目目录的 `.env`，并以操作系统环境变量覆盖其中的同名值。解析过程不修改进程环境，只读取允许的 DeepSeek 与 Pilot 配置项；真实 `.env` 继续由 Git 忽略，`.env.example` 只保存占位符。
- **原因**：将开发密钥限制在项目级配置中，降低全局环境污染，同时保留 CI、临时会话和部署环境使用系统变量覆盖配置的能力。

## ADR-024：Pilot 预算改为逐请求保守预留

- **状态**：已接受
- **日期**：2026-08-06
- **决策**：每次 `/chat/completions` 发送前，以完整请求 JSON 的 UTF-8 字节数加 4096 Token 协议余量作为输入 Token 上界，全部按缓存未命中价格计费，并叠加 `max_output_tokens` 的最高输出费用。只有“已确认成本＋本次最大预留成本不超过配置预算”时才允许发送。响应返回完整用量后用实际估算成本结算；超时、响应用量缺失或无法核对时保留本次最大预留、终止当前 Episode 并冻结所有后续 Chat。预算拒绝发生在 HTTP 调用之前，不产生领域事件或状态变化。2026-08-04 原价格快照保持不变，新门禁使用独立的 `deepseek_v4_flash_pilot_policy_2026_08_06` 快照。
- **原因**：原有门禁只在 Episode 之间检查已返回成本，可能在单个 Episode 内超出预算。逐请求预留使已确认成本与任何尚不确定请求的最坏成本之和始终不超过 1.00 CNY。

## ADR-025：首个真实 Pilot 作为诊断性历史数据保留

- **状态**：已接受
- **日期**：2026-08-06
- **决策**：在用户前一条明确授权下已经完成的首个真实 Pilot 必须按实际结果保留：3 条冻结场景、23 次 Chat、3 条任务 0 条通过、供应商返回成本估算 `0.0332138 CNY`。由于该次运行早于 ADR-024 的严格逐请求门禁，只能作为发现固定课程和评测口径问题的诊断性历史数据，不能作为新门禁的真实验收，也不能据此推进 M3。Fake LLM 的“用量必须未测量”条件不再应用于真实 Pilot 评测。
- **原因**：真实实验数据不能因后续安全收口或授权状态变化而改写为未发生；同时必须明确区分历史运行事实与当前发布门槛。

## ADR-026：真实模型行为探针与 Fake LLM 故障注入契约分离

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：M2b-P0 的 Fake LLM suite 保持冻结，继续验证参考正确轨迹、明确错误轨迹、未知诊断、格式修复和确定性降级。真实模型 Pilot 改用独立的标准完成、错误诱导抵抗和过早诊断/处置安全三探针；三者共用一个病例，只有正向且可达的成功条件。首次合法结构化输出记为格式成功，不要求真实模型主动犯错或触发修复。评测真值和错误对照仍不进入 Agent 上下文。
- **原因**：P0 契约是工程故障注入夹具，直接对真实模型要求“必须修复”或“必须犯错”会把协议成功误判为任务失败，也无法测量真实正向行为。

## ADR-027：调查选项是消耗性领域动作

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：调查成功后立即从 `CaseObservation.available_investigations` 移除。模型伪造或重放已完成的调查 ID 时，`CaseEngine` 使用 `investigation_already_completed` 拒绝，不产生事件、不增加修订且不改变状态。Prompt 只说明工具提交语义和推进原则，不泄露调查顺序、正确诊断、正确处置或评分规则。
- **原因**：重复调查消耗有限步数且没有新信息，原实现还会生成空线索事件，使轨迹长度和行为评测失真。

## ADR-028：`fixed_v0` 的诊断就绪门禁是可替换运行策略

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：M2 的 `fixed_v0` 实验基线在应用工具层注入 `DiagnosisReadinessPolicy`。默认策略只依据权限过滤后的玩家与病例视图；只要活跃病例仍有当前公开、可执行的固定课程调查，就将 `can_submit_diagnosis` 设为 `false`，强行提交时返回应用层错误 `diagnosis_not_ready`，不形成领域命令、事件或状态变化。`CaseEngine`、病例定义、命令和事件不得加入“完成全部调查”、固定调查顺序、固定诊断步骤或正确答案门禁。替换策略后无需修改引擎主体或事件模型。
- **演进边界**：现有命令—事件结构保持诊断阶段中立，允许诊断后继续调查和再次提交诊断，因而可在未来策略与评分层扩展暂定诊断、证据充分度、置信度、提前诊断风险、多路径和多结局。本轮不实现 `adaptive` 策略、风险扣分或新玩法；`fixed_v0` 将当前所有可见调查视为课程要求，仅用于在 8 步内形成可重复 M2 闭环，不是永久通用玩法。
- **原因**：固定课程需要稳定的调查—诊断—处置轨迹，但把实验门禁写入通用引擎会锁死后续自适应教学和玩家主动推理空间。

## ADR-029：提前行动只由策略或规则违规判定

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：真实行为评测中的 `premature_action` 只统计诊断或处置被当前运行策略或确定性规则以 `diagnosis_not_ready`、`evidence_not_discovered`、`diagnosis_required`、`treatment_prerequisite_missing` 等前置条件错误拒绝的步骤。已被规则接受的诊断不得因为没有引用评测器指定的证据全集而判为提前行动。诊断时引用证据数、当时可用证据数和引用覆盖率作为非阻塞描述指标保存，不参与任务通过条件。
- **原因**：行动时机与证据表达质量是两个不同维度。把证据引用完整度混入提前行动会覆盖确定性引擎已经验证的合法状态变化，并把 `resolved / 100` 的产品成功错误改写为任务失败。

## ADR-030：M2 按工程安全证据退出，行为限制作为已知风险保留

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：M2 工程里程碑标记为完成。退出依据是：真实 `deepseek-v4-flash` Adapter 与指标已验证；标准探针在 8 步内完成正确诊断、正确处置和 `resolved / 100`；错误诱导探针没有接受 `evil_spirit_attack` 且提交正确诊断；过早行动探针的未知调查和 4 次 `diagnosis_not_ready` 均被规则层拒绝，没有非法状态写入；最新三探针 24/24 次 Chat 首次结构化成功，事件连续且重放一致；预算、超时、用量缺失和停止门禁均有验证证据。两个安全探针仍按原契约记录任务闭环失败，不改写为成功。单病例各一次不构成正式成功率、跨病例能力或模型完全可靠结论。M2 付费运行和 Prompt 调优停止，三个真实探针不得重跑。
- **评测限制**：固定病例完整闭环需要 8 个有效动作，探针决策上限同为 8；任一规则拒绝或解释性 `respond` 都会使闭环不可达。该限制只解释步骤效率压力，不修改历史成功条件、最终状态或失败分类。
- **M3 入口**：M3 尚未开始。后续 MCP 只包装现有应用服务，必须返回安全结构化错误码和刷新后的权限过滤公开选项，不得绕过 `DiagnosisReadinessPolicy`、规则引擎、`AgentContextFilter` 或领域事件写入；被拒绝调用不得产生事件、修订或状态变化。Agent 拒绝后恢复、解释性对话计步、自适应/暂定诊断、多结局、长期记忆、自适应教学和 Reflection 均不在本轮实现。
- **原因**：M2 的目标是证明真实模型能在确定性安全架构中完成标准闭环，并证明模型受压失误不会污染关键状态，而不是要求单次 V0 Prompt 在所有对抗探针中零冗余、零错误。分开记录模型行为失败与规则安全通过，可以如实关闭工程阶段，同时为 M3 保留不可绕过的边界。

## ADR-031：M3-P0 使用应用 Facade 与官方 MCP v2 进程内契约

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：M3-P0 只增加 `mcp>=2,<3` 这一项直接依赖，并使用官方稳定版 `MCPServer` 创建可注入 Server factory。MCP handler 只负责严格参数接收、调用应用 Facade 和返回统一结构化结果；Facade 复用 `JsonStateStore`、`AgentContextFilter`、`V0ToolExecutor`、`FixedV0DiagnosisReadinessPolicy` 与 `CaseEngine`。状态变化必须来自既有领域事件，只有事件执行和安全返回构建均成功时才保存；参数或规则拒绝、未知内部错误和持久化失败均不返回事件，不增加修订，不覆盖原会话文件。领域拒绝返回稳定安全错误码和刷新后的公开选项，异常文本、路径、隐藏真值与评分规则不得跨越边界。
- **工具边界**：冻结 `get_player_view`、`get_case_observation`、`observe_patient`、`question_patient`、`inspect_object`、`observe_qi`、`investigate_location`、`submit_diagnosis`、`execute_treatment` 九个工具。不增加 Resources、Prompts、Sampling、Roots、Elicitation、长期记忆或新玩法。
- **验证边界**：P0 只用官方 `Client(server)` 做纯进程内发现和调用，不在模块导入时读取 `.env`、启动 stdio/HTTP 服务或建立网络连接。M3-P0 完成不代表真实 MCP Host、stdio、HTTP/SSE、认证或部署可用；这些属于需要另行授权的 M3-P1 及以后阶段。
- **原因**：在引入跨进程传输前先冻结最小公开契约并证明拒绝零写入，可以让未来 MCP Host 复用已验证安全边界，避免在协议层复制或绕过病例规则。

## ADR-032：M3-P1 使用显式本地目录和官方 stdio 生命周期

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：M3-P1 将 SDK 依赖精确固定为 `mcp==2.0.0`，通过 `xuanyi-mcp-stdio` / `python -m xuanyi_npc.mcp_server.stdio` 启动既有 `MCPServer`。启动参数必须显式提供已经存在的病例目录和状态目录；在进入 stdio 传输前校验目录及其中全部病例 JSON。缺少参数、目录无效或病例数据无效时只向 stderr 返回安全诊断并以非零码退出。正常运行时 stdout 只承载 MCP 协议，模块导入不启动服务、不写状态、不读取 `.env` 或访问网络。
- **生命周期**：集成测试必须使用官方 `StdioServerParameters`、`stdio_client` 和 `Client` 启动真实独立 Python 子进程，并设置有界连接、请求和关闭等待。客户端关闭 stdin 后服务器应正常退出；重启的新进程继续通过同一 Facade 和 `JsonStateStore` 恢复状态。测试目录只能是临时目录，被拒绝调用继续保持事件、修订和文件字节均不变。
- **范围边界**：P1 不修改冻结的九个工具、参数 Schema、安全语义、诊断策略或规则引擎，不增加 HTTP/SSE、认证、远程部署、长期记忆、Reflection、自适应诊断或多结局。P1 通过后仍需单独执行 M3 退出审计，不能自行宣布整个 M3 完成。
- **原因**：真实 stdio 子进程引入配置、协议通道纯净性、进程回收和磁盘恢复等进程内测试无法覆盖的风险；显式配置与启动前失败可以避免误用真实状态或进入半启动状态。

## ADR-033：V1 长期记忆使用确定性事件投影和 SQLite 权威存储

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：V1 永久记忆只允许由已经成功提交的 `InvestigationCompletedEvent`、`DiagnosisSubmittedEvent` 和 `TreatmentExecutedEvent`，经过版本化、默认拒绝未知事件的确定性投影生成。应用层必须先把领域事件缩减为不含隐藏字段的 `VerifiedMemorySource`，再写入带来源事件 ID、玩家、会话、序号、修订、投影版本、固定写入原因和内容哈希的 `MemoryEvent`。拒绝动作、只读调用、`respond`、Agent 文本、原始聊天、错误和评测日志不能写入；错误诊断只能记录为玩家提交过的假设，不能保存成真相。V1 自动投影的关系影响为空，`RELATIONSHIP`、`COMMITMENT` 需要未来专用领域事件，`REFLECTION` 不属于 V1。
- **存储决策**：现有 `JsonStateStore` 继续作为玩家和病例会话状态存储；长期记忆、公开来源收据、生命周期审计和删除墓碑使用独立 SQLite 权威库。游戏状态先落 JSON，记忆再以至少一次方式幂等投影；记忆失败形成可协调的 pending 状态，不允许先写记忆后写游戏状态。重复来源使用稳定 ID、投影版本、唯一约束和内容哈希去重；同 ID 不同哈希明确冲突，不静默覆盖。
- **生命周期**：更正创建新记录并使旧记录 `superseded`；撤销/普通删除使记录 `invalidated`；隐私硬删除移除文本、公开来源负载和向量，只保留非内容墓碑以阻止重建复活。向量可以从 active 权威记忆重建，完整投影只能从未被硬删除的安全来源收据重放，不能由模型或向量反推事实。
- **原因**：JSON 快照不适合跨 Episode 查询、唯一约束、事务和删除审计；SQLite 在不引入数据库服务的前提下提供所需可靠性。确定性来源链和幂等协调既避免模型污染，也避免状态/记忆双写失败产生无来源永久事实。

## ADR-034：V1 使用玩家过滤后的进程内余弦 Top-K，真实 Embedding 另行授权

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：V1 向量保存在 SQLite 独立派生表，使用固定维度 little-endian float32 BLOB、`embedding_space_id`、维度、norm 和来源内容哈希；向量不是记忆事实的唯一来源，可整体删除并重建。小规模阶段不引入向量数据库，Repository 必须先按可信 `player_id`、active 状态、允许类型和可见级别过滤，再在进程内计算余弦相似度、应用显式阈值并取 Top-K。排序固定为相似度降序、稳定记忆 ID 升序；并列结果不使用时间、重要度、关系或能力，后者属于 V2 多因素排序。
- **安全与版本边界**：`AgentContextFilter` 同时负责检索前 `MemoryScope` 和检索后最小 `MemoryView`；V1 Prompt 把记忆标为非指令历史数据。V1 继续固定课程，`AgentAction` 和 MCP 工具不增加永久写入口；V0 不构造记忆依赖且对 Repository/Embedding 的调用必须为 0。
- **评测与外部调用**：工程测试使用固定分词、SHA-256 特征桶和 L2 归一化的确定性 Fake Embedding，建立跨 Episode Gold 来验证相关召回、无关排除、空结果、幂等、失效/删除、稳定并列、提示注入数据化、隐藏字段排除、跨玩家串扰为 0 和非法永久写入为 0。真实 Embedding 的供应商、模型、可发送数据、密钥、价格、预算、超时和网络调用必须由用户另行决定并授权；DeepSeek Chat 的历史授权不自动延伸。
- **原因**：先用最小可重建方案证明存储、检索和安全边界，可以避免过早引入向量数据库或供应商锁定，同时保持 V0→V1 只增加基础长期记忆这一实验变量。

## ADR-035：M4-P1 只哈希公开来源负载并限定应用级删除保证

- **状态**：已接受
- **日期**：2026-08-07
- **决策**：`VerifiedMemorySource`、来源收据和全部来源/内容哈希只能基于事件允许列表过滤后的公开规范负载。实现不得保存、序列化或哈希原始 `CaseEvent` 负载，尤其不得让 `diagnosis_correct`、数值评分、正确处置标记、根因、未发现线索或隐藏门槛进入收据、记忆、哈希输入及错误输出。稳定来源 ID 只由公开的事件类型、会话 ID 和序号构成，不包含隐藏字段。
- **生命周期边界**：更正必须从可信应用或管理边界接收稳定唯一的操作 ID、目标玩家、被替代记忆和固定原因码；新建独立替代记忆并在同一事务中把旧记忆标记为 `superseded`，不得用相同来源 ID 覆盖原内容。相同操作 ID 重放幂等，不同操作共用 ID 时明确冲突。
- **删除保证**：隐私硬删除保证目标及其更正派生链的权威内容、公开来源负载、关联实体及未来派生内容从应用 SQLite 数据库原子移除，Repository API 不再可读，投影重建不会复活，且为每条被清除记录只保留不含文本的墓碑。该工程保证不覆盖外部备份、文件系统历史，也不声称取证级物理擦除。
- **原因**：即使只保存哈希，若哈希输入包含隐藏真值，也会扩大秘密数据处理范围并妨碍删除边界审计；明确可信操作身份和应用级删除范围可以避免把逻辑删除误写成不可证明的物理擦除承诺。

## ADR-036：M4-P2 使用确定性 SHA-256 特征桶和显式索引完整性门禁

- **状态**：已接受
- **日期**：2026-08-07
- **Fake 算法**：离线工程测试固定使用 `fake_sha256_token_buckets_v1`：文本经 NFKC、Unicode casefold 与空白折叠规范化，中文按单字、其他 Unicode 单词按词、标点按独立 Token 切分；每个 Token 使用 SHA-256 映射到固定 64 维非负计数桶，最后 L2 归一化。空间 ID 固定为 `fake_sha256_token_buckets_v1_d64`。实现不得使用 Python `hash()`、随机数、pickle、模型用量或供应商元数据；该向量只验证存储、排序和隔离，不代表真实语义能力。
- **迁移与派生数据**：SQLite Schema v2 从 v1 单事务迁移，新增以 `(memory_id, embedding_space_id)` 为主键的 `memory_embeddings`，向量使用 little-endian float32 BLOB，并保存玩家、权威内容哈希、维度、L2 norm 和生成时间。派生行使用外键依附权威记忆；更正、失效和硬删除在原生命周期事务中删除相关向量。迁移失败保持 v1 完整不变，未知未来版本继续拒绝。
- **检索门禁**：索引与检索 API 必须接收精确 `player_id`。检索先核对该玩家全部 active 记忆在指定空间中都有内容哈希有效的向量；缺失或过期时返回 `memory_index_incomplete`，不得伪装成零召回。只有索引完整时，阈值后无命中才表示“没有达到阈值的相关记忆”。排序只使用 `similarity DESC, memory_id ASC`，不读取 importance、时间、关系、能力、类型权重或模型重排。
- **范围原因**：固定 Fake 与完整性门禁可以在零网络条件下证明向量格式、迁移、生命周期、玩家隔离和 Top-K 确定性，同时避免把不完整索引误判成模型无记忆，也避免提前引入 V2 排序或真实供应商依赖。

## ADR-037：V1 记忆采用跨 Episode 双重过滤和独立只读 Prompt

- **状态**：已接受
- **日期**：2026-08-07
- **作用域决策**：V1 `MemoryScope` 只能从匹配的可信 `PlayerState` 与当前 `CaseSessionState` 构造，固定允许 `EPISODIC`、`LEARNING` 并排除当前 `source_session_id`。Repository 仍先按精确玩家和 active 状态取候选，允许类型与当前 Episode 排除必须在索引完整性检查、余弦排序和 Top-K 截取前完成；不能先取 Top-K 再删除越界结果。
- **视图与查询决策**：内部命中在模型调用前由 `AgentContextFilter` 再次核对玩家、类型和来源会话，并缩减为只含不透明 `memory_id`、记忆类型、公开内容和发生时间的 `MemoryView`。`memory_query_v1` 只使用当前用户消息、公开病例标题/简介、已发现线索说明和固定课程，采用固定字段顺序与规范化；病例真值、未发现线索、正确答案、评分、隐藏门槛和其他玩家数据不得进入查询或 Prompt。
- **Agent 决策**：V1 使用独立 `V1DoctorAgentInput` 和 Prompt `v1.0.0`，不向 V0 输入增加可选记忆字段。记忆只作为用户上下文中独立的 `retrieved_memories` JSON 数据，不进入 system message、工具定义或额外消息角色；同一安全上下文必须贯穿一次有界格式修复。V1 与 V0 使用相同固定课程和 `AgentAction`，不存在永久记忆写、删、改工具。
- **失败语义**：`ready` 表示检索成功且有合法历史，`empty` 表示索引完整但没有达到阈值的合法历史；二者可以调用 V1 Agent。索引缺失/过期、存储/检索失败、玩家不匹配或检索后出现跨玩家、当前 Episode、非允许类型时统一返回 `memory_context_unavailable`，不调用 LLM，也不发送部分结果。
- **证据边界**：P3 使用确定性 Fake Embedding 与 Fake LLM，只能证明程序化过滤、消息角色、工具集合、课程和 Schema 没有被注入式记忆文本改变；不声称真实模型已经抵抗记忆提示注入。V0 Prompt `v0.2.1`、`DoctorAgentInput`、9 个 MCP 工具和全部 V0 运行路径保持不变。
- **原因**：排序前过滤避免越界记忆占用 Top-K 或污染索引完整性，检索后二次校验防止错误或伪造的 Retriever 输出进入模型；独立 V1 契约则能明确测量“增加基础长期记忆”这一变量，并保护已冻结的 V0 基线。

## ADR-038：M4-P4 冻结输入/Gold 清单并以逻辑快照判定可重复性

- **状态**：已接受
- **日期**：2026-08-08
- **数据决策**：14 条合成场景输入、Gold 预期和清单分别保存；严格 Schema 禁止未知字段，清单固定场景文件、Gold 文件和检索配置的 SHA-256。Gold 只进入评测比较，不进入投影、查询构建、检索、权限过滤或 Prompt。第一次完整评测前建立本地冻结检查点，后续不得通过调阈值、删场景或改预期掩盖失败。
- **执行决策**：每次完整套件使用新的临时根目录，每条场景使用独立 SQLite/JSON 状态与调用计数。运行器必须经过 P1–P3 的真实程序边界，并把控制性拒绝与实际安全违规分开记录；未知运行错误只返回固定安全类别和原因码，不输出原始异常、路径或堆栈。
- **可重复性决策**：比较有序召回、投影数量、公开内容/来源哈希、生命周期状态、安全计数，以及按表、列和行规范排序的 SQLite/JSON 逻辑快照；不得把 SQLite 原始文件字节当成稳定协议。墙钟延迟、文件大小、Python/操作系统信息只作为观察身份，不进入确定性结果哈希。
- **指标边界**：Precision、Recall、F1 和 False Memory Rate 的分母为 0 时保持缺省。P4 指标只代表冻结合成 Gold 和确定性 Fake Embedding，不能写成真实 Embedding 语义准确率、真实模型长期记忆成功率或生产性能。
- **原因**：将真值、产品路径和运行观察分离，可以防止评测目标泄漏、事后调参和平台页布局干扰，同时保留失败证据与安全门槛的可审计性。

## ADR-039：M4.5 先验证本地 BGE-M3，真实 V1 通过后再进入 M5

- **状态**：已接受（规划；P1 执行仍需用户单独授权）
- **日期**：2026-08-08
- **阶段决策**：M4 保持已完成，不以真实效果缺口重写 Fake Embedding 离线结论。新增 M4.5-P0 规划、P1 真实 Adapter 离线实现与 Mock、P2 小型真实语义 Pilot、P3 DeepSeek Chat + 真实检索 V1 Pilot和退出审计；M5 自适应教学必须等待 M4.5 退出，以免把召回问题与教学策略问题混为同一实验变量。
- **路线决策**：首选在本机以 `BAAI/bge-m3` 的 dense-only、1024 维、固定 revision/SHA、FP32 方式实现现有 `EmbeddingAdapter`。本机 12 GiB 显存、约 31 GiB 内存和约 120 GiB 可用项目盘适合小型 Pilot；本地路线不发送记忆文本且没有长期调用费。阿里云百炼 `text-embedding-v4` 是本地方案失败后的外部首选备选，智谱 `embedding-3` 仅保留技术备选。DeepSeek Chat Key 不得复用，任何 API 账号、条款、密钥和预算都需用户单独提供或授权。
- **数据与空间决策**：M4.5 使用独立、运行前提交冻结的 15 条合成语义 Gold，不修改 M4 的 14 条工程 Gold。只允许公开架空文本；真实玩家身份、聊天原文、隐藏真值、评分、关系、权限和密钥不得外发。Fake 与每个真实模型/precision/revision 使用不同 `embedding_space_id`，不得覆盖、混用或在正式 Pilot 中静默回退。
- **安全与费用决策**：真实向量仍是可删除、可重建的派生数据，不获得权威记忆写权限。P2/P3 继续要求玩家/会话预过滤、索引完整性、`memory_context_unavailable` 安全停止、无自动重跑和请求前预算门禁；Chat 与 Embedding 的调用数、用量和费用独立核算。真实 Adapter 失败不改变权威记忆，可在单独工程演示中显式重建 Fake 空间，但不能污染真实 Pilot 结果。
- **原因**：本地 BGE-M3 在隐私、中文/多语言能力、许可证、硬件适配和现有无角色化 Embedding 契约之间提供最小改动的平衡；保留外部 API 备选可以在 Windows 依赖、性能或质量门槛失败时继续验证，而不把供应商锁定或数据外发提前带入主线。

## ADR-040：本地 BGE-M3 使用固定 safetensors 白名单与设备隔离空间

- **状态**：已接受
- **日期**：2026-08-08
- **模型决策**：M4.5-P1 固定 `BAAI/bge-m3` revision `142964af7e05de16511657561de8e8750fc153a0`、dense-only、FP32、1024 维。只下载 11 个 Sentence Transformers 所需文件，主权重必须是 `model.safetensors` 且 SHA-256 为 `993b2248881724788dcab8c644a91dfd63584b6e5604ff2037cb5541e1e38e7e`；`.bin`、`.pt`、pickle、ONNX、图片、sparse、ColBERT 和 reranker 均禁止。模型及运行结果只保存在 Git 忽略目录。
- **依赖决策**：大型 ML 依赖保留为 `local-embedding` optional extra，Windows/Python 3.12/CUDA 12.6 的可复现锁使用 `torch==2.12.1+cu126`、`numpy==2.4.6`、`safetensors==0.8.0`、`transformers==4.57.6` 和 `sentence-transformers==5.7.0`。基础 `.[dev]` 不自动安装它们；安装只能发生在项目 `.venv`，不得修改全局 Python/Anaconda。
- **加载决策**：Adapter 模块导入和构造不加载 Torch 或模型。显式 `load()` 必须先校验规范 manifest SHA、完整文件集合、每个文件大小/SHA 和安全模块类型，再以 `local_files_only=True`、`trust_remote_code=False`、固定设备和精度加载；失败不自动切换 CPU、模型、精度、revision、API 或 Fake。
- **空间决策**：真实空间 ID 包含 revision 短标识、dense、FP32、维度、设备和最大输入长度；本次烟雾空间为 `bge_m3_142964af7e05_dense_fp32_d1024_cuda_l512_v1`。未经预注册容差与排序一致性证据，不得混用 CPU/GPU 或不同截断配置的向量，也不得覆盖 `fake_sha256_token_buckets_v1_d64`。
- **证据边界**：P1 真实烟雾只证明三条公开合成文本可在网络封锁下产生合规、两轮一致的本地向量；不代表 15 条语义 Gold、Top-K 质量、真实 V1 Agent 行为或产品收益。M4 保持完成，P2 需单独冻结与运行语义 Gold，M5 继续等待 M4.5 退出。

## ADR-041：语义 Gold v2 分离合法负例与实际安全排除

- **状态**：已接受
- **日期**：2026-08-08
- **历史决策**：保留 v1 Gold 冻结、首次停止记录、失败检查点 SHA 及原始 v1 文件。v2 继续使用完全相同的模型可见输入和配置，只新增版本化 expectations/manifest；禁止从停止检查点读取排序、向量或指标来调整 Gold。
- **分区决策**：每个冻结候选必须且只能属于 relevant、semantic negative 或 safety excluded。semantic negative 是合法、active、同玩家、历史 Episode 的普通排序候选，被召回只计算语义 FP；safety excluded 必须使用 `cross_player`、`current_episode`、`superseded`、`invalidated` 或 `hard_deleted` 等明确产品状态原因。v2 删除并严格拒绝歧义字段 `forbidden_candidate_ids`。
- **安全决策**：安全计数以实际 Repository、玩家、来源会话、生命周期和墓碑为准，不因 Gold 标签或高相似度自动产生。安全排除对象正常在余弦排序前过滤时不进入语义 TP/FP/FN；实际越界进入候选或结果才触发安全停止。索引不完整伪装 empty、隐藏泄漏和 Embedding 写权威记忆继续使用原硬门槛。
- **指标决策**：相关返回为 TP，合法 semantic negative 返回为 FP，相关未返回为 FN；空结果可以包含合法负例，只有其分数全部低于冻结阈值才判正确。模型、文本、拆分、Top-K、阈值网格、选择程序、排序和 P1–P4 产品实现均不随本次契约修复变化。
- **原因**：v1 的单一“forbidden”集合混合了语义错误和权限/生命周期越界，使合法高字面负例误触安全停止。v2 将检索质量与安全隔离分开测量，同时保留历史负结果并避免依据真实向量反向调参。

## ADR-042：P2 负结果转为开发集并以派生检索文本和新 holdout 重新验证

- **状态**：已接受（离线诊断与规划；实现和新模型运行仍需单独授权）
- **日期**：2026-08-10
- **历史结论决策**：P2 的工程、安全和重复性通过、语义质量未通过保持不变；Recall@3 `0.8889`、False Memory Rate `5/13` 和安全计数全 0 均不得通过改 Gold、删场景或降门槛重写。原 15 条场景已经被观察并用于根因分析，从本决策起只能作为开发/诊断集。
- **根因决策**：已保存向量证明更正记录和极短相关记录都排在第 4，相关项与合法负例分数区间重叠；绝对阈值、Top-N 或分差只能裁剪现有顺序，不能修复这些排序错误。所有基于现有分数的反事实结果必须标记 `post_hoc_exploratory_only`，不得作为新策略通过证据。
- **产品方向决策**：下一轮优先增加独立、版本化的 `retrieval_query_v2` 和 `embedding_document_v2`。前者只嵌入直接相关的公开检索意图和已发现线索，后者是从权威记忆派生、可重建且带独立哈希的无模板噪声文本；两者都不修改权威记忆、来源链、生命周期或 Agent 最终 Prompt。保守阈值、最大返回数和分差只能由新 calibration 按预注册规则选择。二阶段 reranker 仅在这些最小方案不足时另行调查。
- **验证决策**：新建 36 条未见 holdout，其中 calibration 12 条、final test 24 条；final test 至少 20 条有相关答案，并另含 4 条空结果。输入、Gold、manifest、文本版本、空间 ID、参数网格、指标和停止条件必须在任何新 BGE 推理前冻结；final test 不得反向调参，第二次运行只核对重复性。
- **指标决策**：历史 `False Memory Rate` 名称和数值继续保留。新数据对合法无关召回使用 `irrelevant_retrieval_rate`，与跨玩家、当前 Episode、inactive、删除复活和隐藏泄漏等安全计数分开；所有安全计数仍必须为 0。合成指标不称为产品准确率。
- **范围决策**：本决策不修改当前检索实现、不生成新向量、不下载 reranker、不调用网络/API，也不授权 M4.5-P3 或 M5。

## ADR-043：M4.5-P2c 以版本化派生文本和新 holdout 完成 Dense-only 最后一次验证

- **状态**：已接受
- **日期**：2026-08-10
- **查询表示**：`retrieval_query_v2` 只对当前用户明确检索意图与已发现公开线索说明做 NFKC、casefold、空白折叠和固定 Unicode 码点前缀截断。病例标题因装饰性和跨查询重复而不进入本版本；通用简介、固定课程、JSON 字段名、Prompt 指令及隐藏字段均不进入 Embedding 查询。Agent Prompt 仍接收原有完整安全视图，检索文本不替代 Agent 上下文。
- **文档表示**：`embedding_document_v2` 从对应 `VerifiedMemorySource` 的允许列表公开负载确定性派生，保留公开病例语境、实际行动、线索、假设来源语义、处置公开结果和更正后的内容，移除动作枚举、通用模板套话与“未发现新线索”等低辨识文本。SQLite 权威记忆原文、来源链、生命周期与内容哈希不变；派生表示通过独立空间身份隔离，可删除并从 active 权威记录重建。
- **索引与策略**：SQLite Schema 继续为 v2，无需修改权威表。模型 revision、CUDA/FP32/维度、`retrieval_query_v2`、`embedding_document_v2`、规范化及截断由新的检索身份共同绑定；旧空间向量不会参与 V2 检索，并显式报告 `stale_representation`。保守策略固定提供绝对相似度、最大返回数和最小首二名分差，正式参数只允许由新 calibration 按预注册目标选择，final test 不得参与选择。
- **验证与停止边界**：原 15 条 P2 场景保留为已观察开发/诊断集。新 36 条 holdout 在任何 BGE 运行前单独冻结；之后只允许一组两次正式运行。通过则进入 P3；不通过则如实关闭本轮 Dense-only 优化，不自动增加 reranker、其他模型、向量数据库或更大题库。合成 Gold 指标不得称为游戏产品效果或玩家收益。
- **作品集主线**：项目同时服务 Agent 应用岗与游戏 AI 产品岗。M4.5 收口后主线转向游戏纵向切片：新增至少两个可玩病例、正式 V1 Episode Runner、跨 Episode 记忆闭环与普通用户入口。后续路线图分别标注两类岗位证据贡献。

## ADR-044：M4.5 以 Dense 召回限制关闭，M5 改为确定性多病例纵向切片

- **状态**：已接受
- **日期**：2026-08-10
- **M4.5 终止决策**：M4.5 最终状态固定为 `closed_with_known_dense_retrieval_limitations`，不得描述为全部通过。唯一一组两次新 holdout 运行证明本地 BGE 工程接入、玩家/Episode/生命周期/删除隔离、离线执行和重复性通过；final Recall@1 `0.90`、Recall@3 `1.00`、MRR `0.975`，普通语义 FP `0`。但保守返回门禁 TP/FN 为 `11/11`、micro F1 为 `0.6667`，更正切片 FN 为 `1`，未达到冻结准入线。M4.5-P3 本轮取消，不记录为通过；不再自动运行 BGE、调阈值、引入 reranker、替换模型、向量数据库或扩大合成题库。
- **V1 产品边界**：语义向量记忆默认关闭，仅保留实验身份，不进入正式 Agent Prompt。显式 shadow mode 只可记录权限过滤后的检索候选，不能改变消息、行动、工具、课程、病例或玩家状态。未来只有基于真实多病例轨迹重新立项、冻结 Gold 和获得单独授权后，才可再次验证 Prompt 注入。M4 的 Fake Embedding 工程完成结论和现有安全代码不因本决策删除或改写。
- **M5 路线决策**：历史文档把 M5 暂称为自适应教学，现由本决策明确取代为“三病例游戏纵向切片”。M5-P1 建立普通用户可操作的多病例 Episode Runner，P2 新增两个完整病例，P3 用可信领域/Campaign 事件实现跨 Episode 连续性，P4 装配 no-LLM、Fake、DeepSeek V0 和只记录不注入的 semantic shadow 模式，P5 完成试玩与双岗位验收。自适应教学、Reflection、多 Agent 和网页界面顺延到 M5 之后，当前不重新编号为已启动阶段。
- **确定性记忆决策**：M5 正式玩法中的历史影响必须来自已提交事件投影的结构化公开 `CampaignFact`，并由显式 `CrossEpisodeRule` 读取。病例会话先保存，跨案投影随后幂等提交；拒绝动作零事件、零修订、零状态变化。后续病例可以改变公开对话、提示或可选调查，但基础完成路径不得依赖前案选择，也不得改变隐藏真相、正确诊断或评分规则。语义检索不能作为跨案规则条件。
- **作品集决策**：M0–M4.5 的工具、MCP、安全编排、长期记忆/RAG、评测、成本、重复性和失败分析继续作为 Agent 应用岗证据；M5 新增可玩病例、跨案连续体验、NPC 历史反应、玩家成长、普通用户入口和真实试玩证据，补齐游戏 AI 产品岗主线。任何合成检索指标都不得改称游戏效果、玩家收益或产品准确率。
- **原因**：继续优化单一 Dense 检索会使项目偏离可玩产品，而把未过线的召回接入 Agent 又会产生不可控体验。用确定性事件承载正式连续性，可以在保留全部 Agent 工程与真实负结果的同时，尽快形成可运行、可恢复、可试玩的三病例作品。

## ADR-045：M5-P1 通过应用 Facade 和原子 JSON 提供交互式病例 Runner

- **状态**：已接受
- **日期**：2026-08-10
- **应用边界**：M5-P1 新增启动时全量校验的 `CaseCatalog` 和可注入 `MultiCaseEpisodeService`。CLI 只提交严格输入并展示权限过滤结果；调查、诊断和处置仍转换为既有 `AgentAction`，经 `V0ToolExecutor`、`FixedV0DiagnosisReadinessPolicy` 和 `CaseEngine` 产生领域事件。CLI 不直接修改玩家/会话 JSON，不读取病例真值，也不访问 SQLite 记忆、Embedding 或模型 Adapter。
- **身份与存储**：玩家和 Session ID 分别由可注入 factory 生成，正式实现使用 Identifier 兼容的 UUID；测试使用固定序列。显示名允许重复，但经 NFKC、空白折叠、40 字符上限和控制字符拒绝后保存，玩家选择只使用唯一 ID。`PlayerState` 增加从 `0` 开始的修订字段，P1 不更新玩家成长；Session 每个成功领域事件增加修订并立即通过 `JsonStateStore` 原子保存。
- **拒绝与恢复**：同一玩家/病例只允许一个 active Session。规则拒绝、未知参数、跨玩家恢复、已完成后行动和保存失败均返回稳定安全错误及刷新公开选项；拒绝不保存、不产生事件且原会话文件字节不变。`finish_episode` 只读已完成 Session 并保持幂等，`quit` 不生成事件。病例状态先于任何未来 Campaign 投影；P1 不写 `handled_case_ids`、CampaignEvent、CampaignFact、关系、技能或长期记忆。
- **用户入口**：`xuanyi-play` 与 `python -m xuanyi_npc.cli.play` 只接受已经存在的显式病例/存档目录。病例损坏或配置无效在交互前非零退出；正常 stdout 只显示游戏菜单，启动诊断进入 stderr，EOF、Ctrl+C 和正常退出都保留最后一次成功行动。当前只有旧纸伞病例，但目录、服务和菜单都按多个病例稳定排序，不硬编码单病例。
- **原因**：把普通用户入口限制为应用 Facade 的薄层，可以复用已经验证的规则、权限和事件边界，并用独立子进程证明退出/重启恢复，而不提前引入新病例、Campaign 成长、LLM 或语义记忆风险。

## ADR-046：M5-P2 以纯 CaseDefinition 数据扩展三个独立病例

- **状态**：已接受
- **日期**：2026-08-10
- **内容决策**：正式新增 `gray_hearth_inn` 与 `moon_well_echo`。每案固定为 6 个调查、8 条线索（6 关键、2 误导）、3 个公开诊断、3 个处置和 3 级提示；处置恰好包含一个 `resolved`、一个 `suppressed` 和一个 `worsened`。公开候选保持表面合理，正确性、根因、因果链、隐藏信息和评分判定仍只在可信病例层。
- **路径决策**：每案至少 3 个初始调查，后续依赖由已发现公开线索形成无环图；冻结两种不同合法调查顺序，均以 6 次调查、1 次诊断和 1 次处置产生连续事件 1–8、`resolved / 100`，并可从初始状态完整重放。现有四项初始技能足以完成全部必需调查，任何病例都不依赖前案、关系、成长或语义检索。
- **引擎决策**：P2 只新增严格 `CaseDefinition` JSON、内容测试和设计记录，不修改 `CaseEngine`、命令、事件、评分、诊断就绪策略、`CaseCatalog` 或 CLI，也不按 `case_id` 增加专用分支。目录自动发现三个病例；同一玩家的不同病例使用独立 Session，一个病例完成不修改其他病例或 `PlayerState`。
- **失败与未来边界**：合法错误诊断正常提交且诊断分为 0；压制、恶化、前置不足、重复和未知 ID 均保留原规则语义，拒绝零事件、零修订、文件不变。跨案钩子只记录在设计文档，P2 不创建 CampaignEvent/CampaignFact，不更新成长，也不启用 DeepSeek、BGE 或 semantic shadow。
- **原因**：把玩法扩展限制为数据驱动病例，可以直接证明同一确定性引擎承载不同环境、证人和物证调查，而不把内容需求变成引擎特例，并为 P3 的可信跨案连续性留下清晰边界。

## ADR-047：M5-P3 使用独立可重放 Campaign 与公开处置规则实现知识成长

- **状态**：已接受
- **日期**：2026-08-10
- **权威与来源**：跨案完成摘要、公开事实和知识保存在独立 `campaigns/{player_id}.json`，不复制进 `PlayerState.skills` 或关系。只投影已经原子保存、状态为 completed、玩家/病例/Session 匹配且 ActionRecord 连续的公开收据；模型文本、respond、拒绝、隐藏真值、正确性和语义向量写入为 0。
- **规则与成长**：`cross_episode_rules_v1` 只按来源 case、公开 treatment 和 outcome 匹配。旧纸伞的公开解决处置解锁 `contract_provenance_check`，灰灶的公开解决处置解锁 `handoff_sequence_check`；规则输出限于 CampaignFact、知识、后续公开反应、调查建议和推荐顺序。它不读取分数或诊断正确性，也不修改病例定义、工具、技能、关系、答案或评分。
- **提交与恢复**：病例 Session 先保存，Campaign 后投影；不伪造跨 JSON 事务。后写失败返回 `campaign_projection_pending`，显式协调只从已提交 completed Session 幂等补齐。重复 finish/reconcile 不重复事件或知识；来源缺失、损坏或冲突停止协调，既有 Campaign 不变。
- **产品边界**：推荐顺序不锁关，无前史使用中性反应且三个病例均可完成。CLI 显示历程、完成病例、知识、推荐和安全历史反应。语义记忆继续默认关闭；M5-P3 不接入 Agent Prompt、DeepSeek、BGE、shadow mode、自适应教学或网页。
- **原因**：把正式游戏连续性建立在可审计公开事件上，可在 Dense 召回未过线时仍提供稳定、可解释的角色记忆和成长，同时保留规则层对关键状态的唯一控制权。

## ADR-048：M5-P4a 将行动模式与 semantic shadow 正交隔离

- **状态**：已接受
- **日期**：2026-08-10
- **模式决策**：产品装配使用独立的 `GameplayMode`（manual、Fake、DeepSeek V0）和 `SemanticShadowMode`（off、record-only），默认 manual/off。semantic shadow 不是 Agent，也没有行动或状态写权限。manual 不初始化 LLM、记忆库或 shadow；Fake 和 DeepSeek V0 统一经 `ModeAwareEpisodeRunner` 读取安全视图、生成既有 `AgentAction`，再交给 `MultiCaseEpisodeService`、规则引擎、原子存储和 Campaign 协调。
- **Agent 决策**：Fake 参考脚本只属于测试/演示资产，不进入病例 JSON、公开菜单或真实 Prompt。DeepSeek V0 保持 `v0.2.1` Prompt、Flash-only、一次格式修复、无隐式重试和既有预算/错误契约；真实启动必须同时通过 API Key、显式付费确认、正预算、超时、安全结果目录和模型发现门禁。P4a 只用 MockTransport，不授权真实请求。
- **shadow 决策**：record-only 只在领域事件已经保存后消费权限过滤公开观察，过滤其他玩家和当前 Episode，写入 Git 忽略目录中的脱敏记录后丢弃结果。记录固定声明未注入 Prompt、未影响行动、未影响状态；不保存完整 Prompt、原始聊天、隐藏真值或未发现线索。shadow 失败或安全拒绝不改变病例和 Campaign；off 对 Repository、Embedding、Torch、BGE 与 shadow 文件调用为 0。
- **证据边界**：离线捕获测试证明 shadow off/on 的 Agent 请求、Schema、工具、固定课程、行动、领域事件、终态和 Campaign 一致，DeepSeek Mock 请求不含语义记忆。该证据不代表真实模型完成率或真实 BGE 语义效果。真实 P4b 仍需单独模型与预算授权，M5-P5 尚未开始。
- **原因**：把观察型检索与行动 Agent 分离，可以保留 RAG 工程和真实负结果的展示价值，同时保证未过准入线的 Dense 召回不能暗中影响可玩产品。

## ADR-049：M5-P4c 在状态执行前验证公开行动契约并共享一次修复额度

- **状态**：已接受
- **日期**：2026-08-11
- **问题证据**：`AgentAction.tool_call.arguments` 作为供应商无关 JSON 边界保留通用字典，因此 `question_patient(target_id=...)` 可以通过外层结构校验；M5-P4b 中该错误直到工具执行时才被拒绝。原 Runner 又把 `invalid_tool_arguments`、`diagnosis_not_ready` 等不同拒绝压缩成通用文本，没有返回刷新后的准确工具名、`investigation_id` 和公开诊断就绪状态。规则层保证了零写入，但 Agent 难以在有限步骤内恢复。
- **契约决策**：在 DoctorAgent 与状态服务之间增加基于当前 `CaseObservation` 的通用 `PublicActionContractValidator`。调查仅接受准确 `investigation_id` 并核对工具类型；诊断仅接受公开候选、已发现证据和已开放状态；处置仅接受当前公开 ID。契约层只拒绝提案，不执行命令、不生成事件，也不取代 `V0ToolExecutor`、诊断策略或 `CaseEngine` 的最终裁决。
- **恢复决策**：首次结构有效但不符合当前公开契约的提案可以消费既有的一次修复额度，单独记录为 `action_contract_repair`，与 `format_repair` 区分。修复请求只包含稳定错误码、安全公开说明、诊断就绪状态和刷新后的公开选项；修复后仍不合法时确定性降级且不得第三次调用。正常合法动作不增加请求。
- **历史与产品边界**：P0 冻结评测和既有 Pilot 脱敏轨迹保持原历史语义，不因新同一步修复而改写；该兼容仅存在于内部历史评测组装，不是产品或 CLI 开关。三个病例、Prompt `v0.2.1`、8 步上限、Campaign、评分、答案和诊断策略均不改变，`respond` 也不按病例答案硬禁。
- **证据边界**：离线 Fake/Mock 只能证明共享契约、单次有界恢复、安全反馈和拒绝零写入；不能证明真实 DeepSeek 会采取高效策略。P4b 仍是工程安全通过、灰灶未完成、月井未运行的历史负结果；任何 P4d 都需要单独授权。
- **原因**：把“JSON 结构正确”与“当前公开上下文中可执行”分层，可以在模型提案触达状态服务前提供最小权限纠错信息，同时保留规则引擎作为关键状态的唯一权威。

## ADR-050：M5-P5 以公开应用路径和跨进程恢复关闭本地纵向切片

- **状态**：已接受
- **日期**：2026-08-11
- **验收边界**：P5 新增 `xuanyi-m5-acceptance`，但不复制病例答案、评分或 Campaign 规则。它只调用冻结的 Fake 参考 Agent、`ModeAwareEpisodeRunner`、`MultiCaseEpisodeService`、事件重放和 Campaign 重放；每个有效行动由新的独立 Python 子进程从磁盘恢复后提交。结果使用严格 `m5_acceptance_v1` Schema，仓库外结果只写入 Git 忽略目录。
- **安全决策**：验收显式使用 manual/Fake 与 Mock record-only shadow，不读取 API Key、不初始化 DeepSeek、Torch、BGE 或外部 Embedding。参数错误、过早诊断、重复调查和跨玩家恢复必须同时满足零事件、零修订和 Session 文件逐字节不变；shadow off/on 的 Agent 请求、行动、Episode 和 Campaign 必须相同。
- **历史决策**：P5 只校验 P4b/P4d 原始文件 SHA，不运行模型、不覆盖原始结果。P4b 继续是工程安全通过、灰灶未完成、月井未运行；P4d 继续是一次触发 5 次通用行动契约修复并完成三案的真实案例。两者都不是正式成功率。
- **退出决策**：M5 的 13 项最低条件必须同时有自动测试、命令或文档证据，安全、玩家隔离、恢复、重放或普通用户入口任一失败都不能只靠文档关闭。全部通过后，M5 可描述为“三病例、确定性跨案连续性、可见成长、安全 Agent 工具、持久化重放和普通用户 CLI 的本地游戏 AI 纵向切片”，不得描述为生产级游戏或已验证玩家收益。
- **原因**：把验收建立在普通用户与正式应用路径上，同时保留真实负结果和未验证边界，可以为 Agent 应用岗与游戏 AI 产品岗提供可复现证据，而不为收口临时增加模型、玩法或基础设施。

## ADR-051：M6 以双岗位证据和显式发布授权为边界

- **状态**：已接受（P0 审计完成；身份、版权与发布选项仍待用户决定）
- **日期**：2026-08-11
- **阶段决策**：M6 只把 M0～M5 已完成的本地工程事实整理为 Agent 应用岗和游戏 AI 产品岗两套可核验证据，不重新打开病例、模型、记忆算法、Prompt、网页或玩法开发。P0 只做本地离线审计与规划；P1 处理仓库、安装和 CI；P2 处理首页与演示；P3 处理求职材料；P4 才进行最终发布门禁。
- **身份与历史边界**：当前 52 个提交继续保留 `wyd <wyd@localhost>`。P0 不修改 Git 配置或作者，也不改写历史。由于验证文档广泛引用精确提交哈希，默认建议保留开发期历史，从未来提交开始使用用户确认的公开身份；任何作者改写必须在首次发布前获得专项授权并同步全部证据引用。
- **版权与发布门禁**：仓库当前没有项目 LICENSE，代码、病例文本和文档的复用边界未明确。用户必须在 P1 前决定代码和内容授权、公开身份、项目名称及公开/私有策略；这些决定不能由实现者代选。任何远程仓库创建、推送、Tag、Release 或内容发布都必须等到 P4 且再次获得用户明确授权。
- **证据决策**：公开首页将分别组织 Agent 编排/MCP/安全恢复/评测证据，以及三病例/跨案连续性/可见成长/CLI 证据。真实 P4b 负结果、P4c 通用修复、P4d 单次恢复和 M4.5 Dense 负结果继续保留，且必须与合成指标、正式成功率和玩家收益分开。原始供应商请求 ID、密钥、模型、状态库、真实向量和 Git 忽略结果不得进入公开提交。
- **可复现边界**：现有源码仓库和 Windows Python 3.12 证据不能自动等同于可分发 wheel 或跨平台支持。P1 必须明确 source checkout 与 package artifact 的承诺、解决 `data/` 资源定位、验证无 Key 核心安装，并建立不下载模型、不调用 API 的公共 CI 后，才可进入展示与最终发布检查。
- **原因**：项目已经有足够功能和实验深度；当前最大风险不是能力不足，而是身份、版权、复现和叙事失焦。用显式发布门禁可以在不篡改负结果或扩张功能的前提下，把同一组真实证据安全地服务两个岗位方向。
